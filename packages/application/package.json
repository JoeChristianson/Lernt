{
  "name": "@lernt/application",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "type-check": "tsc --noEmit -p tsconfig.json",
    "lint": "echo \"(lint here)\"",
    "test": "vitest"
  },
  "dependencies": {
    "@lernt/domain": "workspace:^"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^1.6.0",
    "@lernt/ts-config": "workspace:^"
  }
}
