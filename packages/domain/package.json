{
	"name": "@lernt/domain",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"files": ["src"],
	"scripts": {
		"build": "echo '(no build needed)'",
		"dev": "tsc -w --noEmit",
		"type-check": "tsc --noEmit -p tsconfig.json",
		"lint": "echo \"(lint here)\"",
		"test": "vitest"
	},
	"dependencies": {
		"zod": "^3.23.8"
	},
	"devDependencies": {
		"@lernt/ts-config": "workspace:^",
		"typescript": "^5.6.0",
		"vitest": "^1.6.0"
	}
}
